{
  // A static site, declared so the build pipeline has nothing to infer. No
  // "main": there is no Worker script, just the files Astro emits into dist/.
  "name": "sacerdotiza-tarot",
  "compatibility_date": "2026-09-08",
  "assets": {
    "directory": "./dist"
  }
}
